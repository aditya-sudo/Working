const initialState = { API_PATH: "https://jsonplaceholder.typicode.com/users" };

export default (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
